import { flightsLabsProvider } from './flights-labs-provider';

describe('flightsLabsProvider', () => {
  it('should work', () => {
    expect(flightsLabsProvider()).toEqual('flights-labs-provider');
  });
});
